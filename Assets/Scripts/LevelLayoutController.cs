using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelLayoutController : MonoBehaviour
{/// <summary>
/// The purpose of this script is to control the parts of the random level layout generation. 
/// 1. Randomly generate a Start Area
/// 2. Build a random dungeon around that start area with a clear end area
/// 3. after the layout is generated, use prefab room layouts to build the level
/// </summary>

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
